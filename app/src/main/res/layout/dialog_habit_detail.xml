<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:fillViewport="true"
    android:background="@android:color/transparent">

    <LinearLayout
        android:orientation="vertical"
        android:padding="24dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_dialog_rounded"
        android:clipToPadding="false">

        <!-- Habit Name -->
        <TextView
            android:id="@+id/tvHabitDetailName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Habit Name"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#212121"
            android:paddingBottom="16dp"/>

        <!-- Separator -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#E0E0E0"
            android:layout_marginBottom="16dp"/>

        <!-- Up Next -->
        <TextView
            android:id="@+id/tvHabitDetailUpNext"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Up Next:"
            android:textSize="16sp"
            android:textColor="#424242"
            android:paddingVertical="8dp"/>

        <!-- Repeat -->
        <TextView
            android:id="@+id/tvHabitDetailRepeat"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Repeat:"
            android:textSize="16sp"
            android:textColor="#424242"
            android:paddingVertical="8dp"/>

        <!-- Time -->
        <TextView
            android:id="@+id/tvHabitDetailTime"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Time:"
            android:textSize="16sp"
            android:textColor="#424242"
            android:paddingVertical="8dp"/>

        <!-- Reminder -->
        <TextView
            android:id="@+id/tvHabitDetailReminder"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Reminder:"
            android:textSize="16sp"
            android:textColor="#424242"
            android:paddingVertical="8dp"/>

        <!-- Tag -->
        <TextView
            android:id="@+id/tvHabitDetailTag"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Tag:"
            android:textSize="16sp"
            android:textColor="#424242"
            android:paddingVertical="8dp"/>

        <!-- Goal -->
        <TextView
            android:id="@+id/tvHabitDetailGoal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Goal:"
            android:textSize="16sp"
            android:textColor="#424242"
            android:paddingVertical="8dp"/>

        <!-- Separator -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#E0E0E0"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="12dp"/>

        <!-- Buttons -->
        <LinearLayout
            android:orientation="horizontal"
            android:gravity="end"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:spacing="16dp">

            <TextView
                android:id="@+id/btnDetailEdit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Edit"
                android:textSize="16sp"
                android:textColor="@color/purple_700"
                android:padding="12dp"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true"/>

            <TextView
                android:id="@+id/btnDetailDelete"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Remove"
                android:textSize="16sp"
                android:textColor="@android:color/holo_red_dark"
                android:padding="12dp"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true"/>
        </LinearLayout>

    </LinearLayout>
</ScrollView>
